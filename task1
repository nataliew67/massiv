//Создать программу, выполняющую следующий функционал:
//- запрашивается количество строк и столбцов для двумерного массива
//- вводится необходимое количество трехзначных чисел (числа могут повторяться)
//- подсчитывается количество различных цифр в полученном массиве
//- на консоль выводится двумерный массив из введенных чисел и количество различных цифр используемых в данном массиве
//Например, для массива
//100   951   101   950
//519   999   155   501
//510   911   144   554
//выведется результат: В массиве использовано 5 различных цифр

fun main() {
    println("Введите кол-во строк массива:")
    val stroki = readln().toInt()
    println("Введите кол-во столбцов массива:")
    val stolbets = readln().toInt()

    val mas =Array(stroki){ IntArray(stolbets)}
    var str=""
    println("Введите 3-х значные элементы массива через Enter")
    for (i in 0..<stroki){
        for(j in 0 ..< stolbets){
            var number = readln().toInt()
            while (number >= 1000 || number < 100) {
                println("Некорректно веденные данные. Введите трехзначное число:")
                number = readln().toInt()
            }
            mas[i][j]=number
            str+=mas[i][j].toString()
        }
    }
    println("Ваш 2-мерный массив: ")
    for (rows in mas) {
        for (element in rows) {
            print("$element ")
        }
        println()
    }
    println("В массиве использовано ${str.toSet().size}\" различных цифр")    //toSet удаляет повторяющиеся элементы
}
